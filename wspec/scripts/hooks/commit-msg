#!/usr/bin/env sh
set -eu

WSPEC_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"

if ! command -v node >/dev/null 2>&1; then
	exit 0
fi

exec node "$WSPEC_DIR/mcp/dist/cli.js" hook:commit-msg "$@"
